// Name: Chufan Deng
// Loginid: chufande
// CSCI 455 PA5
// Spring 2017

// pa5list.cpp
// a program to test the linked list code necessary for a hash table chain

// You are not required to submit this program for pa5.

// We gave you this starter file for it so you don't have to figure
// out the #include stuff.  The code that's being tested will be in
// listFuncs.cpp, which uses the header file listFuncs.h

// The pa5 Makefile includes a rule that compiles these two modules
// into one executable.

#include <iostream>
#include <string>
#include <cassert>

using namespace std;

#include "listFuncs.h"

int main() {

    // ListType list1;
    // initList(list1);
    //
    // insertFront(list1, "Bob", 1);
    // insertFront(list1, "Dav", 2);
    // insertFront(list1, "Cal", 3);
    // insertFront(list1, "Fal", 4);
    // insertFront(list1, "Fal", 5);
    // cout << "a list: " << endl;
    // printList(list1);
    //
    // cout << "lookup: " << *lookup(list1, "Cal") <<endl;
    //
    // changeValue(list1, "Bb", 11111);
    // printList(list1);
    //
    // remove(list1, "Bob");
    // remove(list1, "Fal");
    // printList(list1);
    //
    // ListType list2;
    // initList(list2);
    //
    // printList(list2);
    // remove(list1, "Fal");
    // printList(list2);
    // if(lookup(list2, "Cal")){
    //     cout << "lookup: " << *lookup(list2, "Cal") <<endl;
    // }
    // else{
    //     cout << "No such name" << endl;
    // }
    // changeValue(list2, "Bb", 11111);
    // printList(list2);

    return 0;
}
